package edu.umass.cs.iesl.watr
package display

import org.scalatest._

import textreflow._

class LabelWidgetsSpec extends FlatSpec with Matchers with PlainTextReflow {

  // behavior of "modifying chars"

  it should "specify widget layout" in {

    // val w1 = overlay(
    //   selectTR(sel0),
    //   selectTR(sel1),
    //   targetImage(region0)
    // )
    // val w2 = overlay(
    //   selectRange(IntRange(3, 6)),
    //   reflow(reflow0)
    // )

    // val row1 = row(w1, w2)

    // // approve all selections within layout regions:
    // button(approveSelections(w2))
    // button(approveSelections(row1))

  }
}
